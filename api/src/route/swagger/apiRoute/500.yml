Display a generic 500 error.  This endpoint is a template for other 500 errors in the API.
---
produces:
  - application/json
tags:
  - Error
responses:
  404:
    description: A generic 500 error response from the SaintsXCTF API.